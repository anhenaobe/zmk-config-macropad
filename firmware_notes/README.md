# Firmware Notes

Use this directory for concise, hardware-specific observations that do not belong in the ZMK configuration itself. Record the firmware artifact filename, commit SHA, controller revision, flashing result, BLE host, observed behavior, and any follow-up action.

Do not store generated `.uf2`, `.hex`, or `.bin` files here; retrieve firmware from the corresponding GitHub Actions run.

